<table >
  <div class="col-md-8 col-md-offset-2">
    <div class="col-md-6">
      <img src="<%= @product.image.medium %>" alt="" />
    </div>

    <div class="col-md-6">
      <h1><%= @product.title %></h1>
      售价：<%= @product.price %>
      <br>
      库存：<%= @product.quantity %>
    </div>
  </div>
</table>
